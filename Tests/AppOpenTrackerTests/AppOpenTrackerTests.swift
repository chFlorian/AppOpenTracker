    import XCTest
    @testable import AppOpenTracker

    final class AppOpenTrackerTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(AppOpenTracker().text, "Hello, World!")
        }
    }
